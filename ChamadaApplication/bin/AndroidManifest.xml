<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.chamada"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name="com.application.chamada.activity.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.application.chamada.activity.DisciplinasCadastradasActivity" />
        <activity
            android:name="com.application.chamada.fragment.CadastrarDisciplinaFragment"
            android:label="@string/cadastrarDisciplina" />
        <activity
            android:name="com.application.chamada.fragment.CadastrarHorarioFragment"
            android:label="@string/cadastrarDias"/>
    </application>

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

</manifest>